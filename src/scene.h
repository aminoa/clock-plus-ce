// scene.h - Scene/background drawing
#ifndef SCENE_H
#define SCENE_H

#include <stdint.h>

// Draw the complete scene (clouds/stars, trees, ground, flowers)
void scene_draw(uint8_t is_night, uint8_t frame);

#endif
